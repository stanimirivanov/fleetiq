apiVersion: v1
kind: Secret
metadata:
  name: mqtt-credentials
  namespace: fleetiq
type: Opaque
stringData:
  # Renderable placeholders only. Replace through a secret-managed overlay.
  telemetry-ingestion-password: replace-before-deploying
  device-registry-password: replace-before-deploying
  fleet-topology-password: replace-before-deploying
  streaming-hub-password: replace-before-deploying
