apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: telemetry-ingestion
  namespace: fleetiq
  labels:
    app: telemetry-ingestion
    component: ingestion
  annotations:
    autoscaling.knative.dev/min-scale: "1"
    autoscaling.knative.dev/max-scale: "10"
    autoscaling.knative.dev/target: "100"
    autoscaling.knative.dev/metric: concurrency
    autoscaling.knative.dev/scale-down-delay: "5m"
spec:
  template:
    metadata:
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/q/metrics"
        prometheus.io/port: "8081"
        sidecar.istio.io/inject: "true"
        argocd-image-updater.argoproj.io/image-list: telemetry-ingestion=ghcr.io/fleetiq/telemetry-ingestion
        argocd-image-updater.argoproj.io/update-strategy: semver
    spec:
      serviceAccountName: telemetry-ingestion
      containers:
        - image: ghcr.io/fleetiq/telemetry-ingestion:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8081
              name: http
          env:
            - name: FLEETIQ_MQTT_HOST
              value: mosquitto.fleetiq.svc.cluster.local
            - name: FLEETIQ_MQTT_USERNAME
              value: telemetry-ingestion
            - name: FLEETIQ_MQTT_PASSWORD
              valueFrom: {secretKeyRef: {name: mqtt-credentials, key: telemetry-ingestion-password}}
            - name: QUARKUS_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: postgres-secrets
                  key: username
            - name: QUARKUS_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-secrets
                  key: password
            - name: JAVA_OPTS
              value: "-Xms128m -Xmx512m"
          volumeMounts:
            - name: config
              mountPath: /work/config
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          livenessProbe:
            httpGet:
              path: /q/health/live
              port: 8081
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /q/health/ready
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: config
          configMap:
            name: telemetry-ingestion-config
